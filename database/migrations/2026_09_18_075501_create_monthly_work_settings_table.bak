<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     *
     * @return void
     */
    public function up()
    {
        Schema::create('monthly_work_settings', function (Blueprint $table) {
            $table->id();
            $table->string('month', 7)->unique()->comment('YYYY-MM');
            $table->decimal('standard_days', 4, 1)->default(0)->comment('Số công chuẩn');
            $table->integer('public_holidays')->default(0)->comment('Ngày nghỉ lễ');
            $table->text('notes')->nullable()->comment('Ghi chú phép năm, v.v.');
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     *
     * @return void
     */
    public function down()
    {
        Schema::dropIfExists('monthly_work_settings');
    }
};
